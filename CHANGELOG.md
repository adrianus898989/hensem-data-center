# V252

- 查询时保留当前页面和当前结果，不再因为上一查询为 0 行而整页空白。
- 首次进入仅显示轻量加载条，页面结构保持可见。
- 管理后台展开子菜单字号和点击区域增大。
- 不修改 Supabase RPC、Cron、费率或业务统计逻辑。

## V247 - Supabase 稳定读取 + Profile 顶部 UI

- 三方量页面只读 Supabase；页面打开/查询不会现场读取 Google。
- 修复 Supabase/Netlify 瞬时请求失败或偶发空结果时，页面从正常数据突然变成 0 行的问题。
- 当前选择日期已有成功数据时，失败/空结果永远保留上一份成功数据，不用重新等待 Google。
- 0 行结果不再覆盖浏览器 last-good 缓存；首次异常空结果自动轻量重试一次。
- Supabase 1000 行分页由串行改成最多 4 页并发，单日/昨日对比读取明显更快，不改变业务统计口径。
- 用户 Profile 强制固定到右上角，升级为标准后台账号菜单；底部不再出现账号退出条。
- 保留 V246 登录、权限、Admin 后台、历史自动补齐、费率 Cron 和 v239 三方业务逻辑。

---

# V246 - Profile / 默认昨天 / Supabase 历史自动补齐

- 三方量首次进入默认昨天，按选择日期/月份读取 Supabase；空数据不整页报错。
- 移除首页与三方量页面技术调试状态卡。
- 用户信息移动到右上角 Profile 菜单，新增个人资料与用户自行修改密码。
- Admin 数据后台增加历史补齐进度。
- bright-responder 新增历史自动补齐：每次最多同方向连续 3 天；成功后写入并封存。
- pg_cron 每 5 分钟推进历史补齐，全部成功后自动停止；费率每 6 小时自动同步。

---

## V245
- 移除首页「Supabase 数据状态」卡片。
- 不影响 Supabase 自动同步、三方量/费率读取、Admin 后台手动刷新。

# CHANGELOG

## V238 - 人工确认保留 + 0手续费 + 三模块历史月入口

- 三方量读取层不再过滤明确的 `人工确认` / `人工充值`；所有 AR 站点原始三方列出现人工确认时正常保留为独立行。
- 人工确认仍优先匹配 Google 费率表；对应方向没有任何费率/单笔费时明确按 `0` 手续费展示，不给其它真实三方擅自补 0。
- `人工确认` / `人工充值` 在费率匹配名称层保持原名，避免被错误文本过滤器转成“未知三方”。
- 自动出款 / 提现操作人、工单、三方量统一增加 `快照月份` 入口：4月起至当前月，当前月/结算中月份有清楚标签；点击历史月只读取该月快照。
- 历史后台先尝试从旧大快照一次性拆出所有已封存月份，仅访问 Netlify Blobs；旧快照确实没有时才按原规则每次最多补一个 Google 历史月。
- 重置历史迁移轻量标记到 V238，使部署后可重新执行一次 Blob-only 迁移；已有历史月快照仍永久只读，不覆盖。
- 当前月每小时同步、上月 1-7 日结算、第 8 日封存、低流量状态检查与失败不覆盖旧快照逻辑保持不变。

## V237 - 当前月小时快照 + 历史封存 + 小状态低流量

- 自动出款、工单、三方量、客服统一数据生命周期：当前月每小时更新；每月 1-7 日上月处于结算期；第 8 日起永久封存。
- 普通页面打开只读当前月一个月；月初当前月暂时无数据时，只回退上月结算快照，不再把两个月整包一起下发。
- 新增小型 `monthly-status` 状态接口：页面每小时只检查 checksum / 最后检查时间，数据没变化不重新下载大 JSON。
- 当前月数据真正变化时才生成带版本号的请求 URL，立即绕过旧 CDN；同版本反复 F5 继续命中浏览器/CDN 缓存。
- 三方量右上角拆分为“最后检查”和“数据更新”，避免数据没变化时误以为小时任务没有执行。
- 当前月/结算月缺快照时不再扫描旧大快照；只有已封存历史月缺失时才允许一次性从旧快照迁移。
- 历史月已有月快照后永久只读；4/5/6 等月份不会参加每小时 Google Sheet 读取。
- 保留 V236/V234 的费率表读取、手续费计算、三方别名、失败不覆盖旧快照等业务逻辑。
- 删除分散的 `CHANGELOG_Vxxx.md`，以后只维护本文件。

## V224 - 工单后台化与历史月主动补齐

- 工单 Scheduled Function 改为只排队，真正读取两个大型 RAW 表交给 Background Function，避免网页接口/定时函数超时后返回空内容。
- 工单页面接口只读快照；没有快照时自动排队并每60秒重试，浏览器旧成功缓存可继续显示。
- 三方量页面发现 2026-04/05/06 缺失时，自动排队历史月后台任务；页面每60秒重读，成功月份立即封存。
- 历史月采用“一个模块 + 一个月”单任务、游标轮转和运行锁；失败会绕过后续再重试，不会卡住整个队列。
- 月初保留上月结算：前2天按6小时更新，第3天最终封存。
- 自动出款页面移除顶部技术说明黄色长条。
- 保留 ArbPay 代收 1.5%，OX2PAY 统一为 OXPay。

## V222 - 三模块稳定修复与三方量轻量化

- 自动出款“上月”改为相对当前选中月份递减，支持连续 7→6→5→4 月；不再每次按系统今天重置到 6 月。
- 工单固定使用 `workOrderSourceConfig.ts` 中两个真实 RAW 表，忽略 Netlify 遗留旧 ID；无页签或解析 0 行时抛出详细诊断，失败不覆盖旧有效快照。
- 三方量新增 `latest-full` 当前月完整后台任务；页面过期自动恢复和备用按钮都跑完整当前月循环，不再只补一片。
- 当前月固定取消列分片，只按页签分片；解决纵向/窄表第二列分片为空、游标空转的问题。
- 三方量快照在写入前按精确分片和业务维度聚合压缩，大幅减少 58,000+ 行快照重复合并/写入的压力。
- 完整任务链在最后一片前不释放锁，并在每片开始更新心跳，避免定时任务和手动任务并发覆盖。
- 任务状态与锁改为严格写入；不再静默吞掉 Blob 写入失败，也不再用新的 skipped 状态覆盖真实运行进度。
- 印度 ArbPay 代收手续费固定为 1.5%；OX2PAY 与 OXPay 统一为 OXPay。

## V221 - 三方量改为真正后台自动任务

- 找到 V220 仍然失败的根因：Netlify Scheduled Function 实际只有 30 秒执行上限，但旧代码仍在定时函数里直接读取 Google Sheet、合并 5 万+ 行并写两份大快照；所以定时任务超时，更新时间停住。
- 手动按钮原来调用同步 Next API；超时后 Netlify 返回 HTML 错误页，前端把它显示成“三方量刷新接口返回格式异常”。
- 新增 `sync-third-party-volume-background`：Google Sheet 读取改由最长 15 分钟的 Background Function 执行；定时函数只排队，页面关闭也不会中断。
- 当前月每 15 分钟自动排队一片；历史月每小时自动排队一片。
- 页面发现快照超过 30 分钟未更新，会自动触发一次后台恢复，并每 60 秒重读轻量快照。
- “立即后台补一次（可选）”改为一次排队；全部分片由后台逐片链式执行，不再让浏览器同步跑 10 次。
- 修复游标最后一个漏洞：必须等 Netlify Blobs 大快照写成功以后才推进；写入失败/超时不会跳片。
- current 与 last-good 两份大快照改为并行写，降低写入耗时。
- 自动出款逻辑未改。

## V220 - 修复三方量自动卡住与按钮假完成

- 修复真正根因：旧版在 Google Sheet 读取前就推进分片游标；读取超时/失败后会永久跳过该分片。现在只在读取正常返回后推进，失败会停在原分片自动重试。
- 修复按钮“按了像没反应”：站内按钮改用同源 POST；即使 Netlify 设置了 `SNAPSHOT_REFRESH_TOKEN` 也可正常执行。
- 修复按钮失败后仍显示“刷新完成”：现在任何分片失败都会明确停住并显示真实错误，不再假完成。
- 手动刷新增加 55 秒单片超时、清楚的总进度与成功/失败提示；已成功分片会保留并立即重新加载。
- 历史月自动任务从每小时 4 次降为每小时 1 次，避免与当前月分片争抢 Google Sheet 读取额度；当前月仍每 15 分钟 1 片、1 小时合并完整。
- 三方量所有实际读取都失败时直接抛错，失败不覆盖旧快照；来源表没有对应月份时按正常空片跳过，不会卡住，也不会伪造更新时间。

## V219 - 单按钮补全部三方量

- 三方量右上角只保留一个按钮：`补全部 / 立即更新`，点击后自动跑当前月分片 + 历史月分片。
- 保留自动同步逻辑：当前月三方量每 15 分钟跑一片，一小时内合并完整；历史月独立补读。
- 失败不覆盖旧快照，页面继续显示最后一次成功数据。
- 不新增分散的 `CHANGELOG_Vxxx.md`，以后统一写入这个 `CHANGELOG.md`。

## V218
- 三方量页面右上角新增“刷新当前月 / 补历史月 / 一键刷新”按钮，不用等 Netlify 下一次定时。
- 手动刷新会调用现有 snapshot-refresh 接口，连续跑当前月分片和历史月分片，完成后自动重新读取页面快照。
- 保留 V217 的失败不覆盖旧快照、4/5/6 历史补月、同三方别名规则。

## V217 - sync source history fix

- 修复三方量分片合并时把同页签其它国家区块误删的问题：现在按「来源表 + 页签 + range」替换，不会漏国家。
- 修复三方量历史月份读取：`sync-third-party-volume-history` 不再被 `THIRD_PARTY_VOLUME_SCHEDULE_MONTHS=2026_07` 卡住，默认自动补 2026-04 到上个月。
- 三方量当前月默认每 15 分钟读取一个小片，1 小时内合并完整；失败保留旧快照，不覆盖 0。
- 修复 `parseThirdPartyVolume.ts` 重复声明导致构建风险的问题。
- 巴西别名补充：`PIXPAY20 / PIX20 / DyPayV2` 统一显示 `DyPayV2`；`dp-vpsPay / wd-vpsPay` 继续统一为 `VpsPay`。
- 自动出款、工单、费率、客服定时函数版本日志统一到 V217。

## 保留逻辑

- 页面打开只读最后成功快照，不现场重读 Google Sheet。
- 后台 scheduled function 自动刷新；成功才覆盖，失败不覆盖旧数据。
- 三方量支持多个 `THIRD_PARTY_VOLUME_SHEET_ID / THIRD_PARTY_VOLUME_SHEET_IDS`，用逗号或换行分隔。
- 自动出款支持多个 `AUTO_WITHDRAW_RAW_SHEET_ID / AUTO_WITHDRAW_RAW_SHEET_IDS`，不存在的页签跳过。

## V239 — 当前月/结算月每小时 + 历史快照只读

- 不改统计口径、手续费算法、人工确认规则、三方别名、UI、Google Sheet 来源。
- 当前自然月每小时更新。
- 每月 1-7 日，上个月处于结算中，与当前月一样每小时更新，确保月底迟到数据持续补齐。
- 每月 8 日起，上个月及更早月份永久历史快照，只读、不更新、不覆盖、不参与定时 Google Sheet 读取。
- 修复定时任务只检查“当前月新鲜度”导致结算月可能被错误跳过的问题：现在当前月和结算月都新鲜时才跳过重复读取。
- 删除每 6 小时的历史自动补建任务；历史查询只读月快照，缺失时最多从旧 Netlify 大快照拆分一次，不会因页面打开去读取 Google Sheet。

## V243 - Hensem Data Center / Admin Control Center
- 新增专业登录页。
- 新增 Admin Control Center：账号权限、数据刷新、后台操作记录。
- Viewer 支持按模块授权；Viewer 永远不能建立账号或手动刷新数据。
- Admin 首页新增“刷新最新数据”。
- 新增 dashboard_audit_log 与权限 RLS 升级 SQL。

## V249 - Owner/Admin/Viewer 权限层级 + 独立管理后台
- 新增唯一 Owner（总管理员），当前 admin 通过升级 SQL 提升为 Owner。
- 只有 Owner 可以建立/修改/删除小管理员。
- 小管理员可单独分配：Viewer 账号管理、数据刷新、操作记录权限。
- Admin/Viewer 均可按业务模块分配查看权限。
- 新增账号删除；Owner 账号不可被后台删除或停用。
- 管理后台从弹窗改为独立全屏工作区，使用左侧管理导航。
- 主业务侧边栏新增「系统管理 → 管理后台 / 个人资料」。
- 本版不修改三方量统计、费率、Cron 与 Google→Supabase 同步逻辑。

## V251 - 真正高速查询 / 数据完整状态 / 内嵌管理菜单
- 修复 V250“高速 RPC 未实际接入前端”的问题：三方量 API 现在真正调用 `dashboard_third_party_volume_fast_v2`，不再走 1000 行分页。
- 月份查询会把当前国家直接传给 Supabase，在数据库侧先缩小国家范围并按 v239 dashboard key 聚合，再一次返回。
- 初次加载不再使用 `min-height:100vh` 巨型空白等待页，改为紧凑加载卡；查询时保留当前结果。
- 新增真实数据完整状态：历史范围直接读取 backfill 成功任务数，60/60 且失败 0 才显示“数据已补齐”；当前月显示代收/代付实际入库日数与最后写库时间。
- 管理后台不再跳转独立全屏工作区，也不使用管理概览：左侧“管理后台”点击后展开“账号与权限 / 数据同步 / 操作记录”，内容在现有后台主区域内显示。
- 登录账号连续 1 小时没有点击、键盘、触摸或滚动操作会自动退出，必须重新登录。
- 三方费率浏览器缓存升级到 V251；用户点击“查询”时强制重新读取 Supabase 费率，手动同步费率后不再被旧浏览器缓存挡住。
